{
  "name": "@extramundum/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --build && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "build:render": "tsc -p tsconfig.render.json"
  },
  "dependencies": {
    "@extramundum/shared": "workspace:*",
    "three": "^0.181.1",
    "zod": "^4.1.13",
    "@extramundum/data": "workspace:*"
  },
  "devDependencies": {
    "@types/three": "^0.181.0",
    "vite": "^7.2.4"
  }
}
